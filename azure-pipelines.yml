
# A pipeline with no CI trigger
trigger:
- none
pool:
  name: 'linux_ob'
- task:- task: SSH@0
  inputs:
    sshEndpoint: 'ANSIBLEVMSSH'
    runOptions: 'commands'
    commands: 'pwd'
    readyTimeout: '20000'SSH@0
  inputs:
    sshEndpoint: 'ANSIBLEVMSSH'
    runOptions: 'commands'
    commands: 'pwd'
    readyTimeout: '20000'

 



