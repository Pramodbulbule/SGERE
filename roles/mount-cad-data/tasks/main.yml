- name: Create fuse tmp directory if it does not exist
  file:
    path: /mnt/azure/cad-fusetmp
    state: directory
    mode: '0600'

- name: Create fuse_connections directory if it does not exist
  file:
    path: /etc/fuse_connections
    state: directory
    mode: '0755'

- name: Copy config
  copy:
    src: includes/fuse_connections/cad_data.cfg
    dest: /etc/fuse_connections/cad_data_fuse.cfg
    group: root
    mode: '0600'

- name: Create mount directory if it does not exist
  file:
    path: /mnt/azure/cad
    state: directory
    group: developers
    mode: '0600'

- name: Mount models storage from the cloud
  shell: blobfuse /mnt/azure/cad --tmp-path=/mnt/azure/cad-fusetmp --config-file=/etc/fuse_connections/cad_data_fuse.cfg -o attr_timeout=240 -o entry_timeout=240 -o negative_timeout=120 -o nonempty
  become: true
