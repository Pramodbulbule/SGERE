# Install iotedge
- hosts: "{{ variable_hosts | default('all') }}"
  become: true
  roles:
    - iotedge-remove
    - ms-repo
    - moby
    - docker-daemon-configuration
    - iotedge
    - iotedge-connect
